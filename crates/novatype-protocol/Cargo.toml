[package]
name = "novatype-protocol"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
bincode = "1"
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
